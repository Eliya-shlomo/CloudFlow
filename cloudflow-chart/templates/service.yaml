apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudflow-chart.fullname" . }}
  labels:
    {{- include "cloudflow-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudflow-chart.selectorLabels" . | nindent 4 }}
